<% content_for :titulo do %>
  Chamados
<% end %>

<% content_for :sub_titulo do %>
  Concluir Chamado
<% end %>

<% if @chamado.status == "EM ATENDIMENTO" %>

	<%= render 'form_concluir_chamado' %>

<% else %>
	<div class="alert alert-warning" role="alert">Este Chamado não pode ser concluido neste momento.</div>
	<%= link_to 'Voltar', chamados_path, :class=>'btn btn-default' %>
<% end %>