<%= simple_form_for(@status_equipamento) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :status %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Salvar", :class=>'btn btn-primary' %>
    <%= link_to 'Voltar', status_equipamentos_path, :class=>'btn btn-default' %>
  </div>
<% end %>
