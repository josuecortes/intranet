<p id="notice"><%= notice %></p>

<h1>Listing Passengers</h1>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>Tipo</th>
      <th>Matricula</th>
      <th>Cpf</th>
      <th>Respaldo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @passengers.each do |passenger| %>
      <tr>
        <td><%= passenger.nome %></td>
        <td><%= passenger.tipo %></td>
        <td><%= passenger.matricula %></td>
        <td><%= passenger.cpf %></td>
        <td><%= passenger.respaldo %></td>
        <td><%= link_to 'Show', passenger %></td>
        <td><%= link_to 'Edit', edit_passenger_path(passenger) %></td>
        <td><%= link_to 'Destroy', passenger, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Passenger', new_passenger_path %>
