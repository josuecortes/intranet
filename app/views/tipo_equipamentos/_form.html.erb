<%= simple_form_for(@tipo_equipamento) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :tipo %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Salvar", :class=>'btn btn-primary' %>
    <%= link_to 'Voltar', tipo_equipamentos_path, :class=>'btn btn-default' %>
  </div>
<% end %>
